# EmployeeManagementSystem
Employee Management System is a Java web application built using JSP, Servlets, JDBC, and MySQL. It features Admin Registration/Login, secure authentication, employee management with CRUD operations (Add, View, Update Salary, Delete), session handling, and a responsive user interface for efficient employee record management.
